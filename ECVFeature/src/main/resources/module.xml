<?xml version="1.0" encoding="UTF-8"?>

<module xmlns="urn:jboss:module:1.2" name="${project.groupId}.${project.artifactId}" slot="${project.version}" >
    <resources>
        <resource-root path="${project.build.finalName}.jar"/>
    </resources>
    <dependencies>
    	<module name="javax.servlet.api" />
    	<module name="javax.jws.api" />
    	<module name="javax.annotation.api" />
    	<module name="com.paypal.infra.nextgen.services.api.CALServiceMock" slot="${project.version}" />
 	</dependencies>
</module>

 